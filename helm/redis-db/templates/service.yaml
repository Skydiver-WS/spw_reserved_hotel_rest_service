apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - port: {{ .Values.service.port }} #Стучится сюда приложение
      targetPort: {{ .Values.service.port }} #Порт самой БД
