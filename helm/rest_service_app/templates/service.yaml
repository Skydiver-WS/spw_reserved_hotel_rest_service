apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest_service_app.fullname" . }}
  labels:
    {{- include "rest_service_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rest_service_app.selectorLabels" . | nindent 4 }}
